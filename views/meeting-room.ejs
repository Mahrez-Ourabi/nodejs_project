<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Meeting Room Details</title>
  </head>
  <body>
    <h1>Meeting Room Details</h1>
    <% if (typeof error !== 'undefined') { %>
    <p>Error: <%= error %></p>
    <% } else { %> <% if (meetingRoom) { %>
    <p>Name: <%= meetingRoom.name %></p>
    <p>Capacity: <%= meetingRoom.capacity %></p>
    <p>Equipment: <%= meetingRoom.equipment %></p>
    <% } else { %>
    <p>Error: Meeting room not found.</p>
    <% } %> <% } %>
  </body>
</html>
